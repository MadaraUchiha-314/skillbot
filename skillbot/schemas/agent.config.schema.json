{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://github.com/MadaraUchiha-314/skillbot/blob/main/docs/schemas/agent.config.schema.json",
    "title": "Agent Configuration",
    "description": "Schema for agent.config.json - per-agent configuration file. The agent graph is defined in a sherma YAML file; this config provides skill directory mappings.",
    "type": "object",
    "properties": {
        "skills": {
            "type": "object",
            "description": "Map of skill name to skill directory path.",
            "additionalProperties": {
                "type": "string"
            }
        },
        "agent-yaml": {
            "type": "string",
            "description": "Optional path to a sherma agent YAML file, relative to this config directory. If omitted, the built-in default agent.yaml is used."
        }
    },
    "additionalProperties": false
}
